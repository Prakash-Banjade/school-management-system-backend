1. Implement caching. 
2. While adding class routines, check if the time overlaps with any existing class routines. Also in frontend, it will be better to not let user select the reserved class times.
3. No more than 1 attendance per day per student.